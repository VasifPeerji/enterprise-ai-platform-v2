fastapi>=0.136.0,<0.137.0
# starlette MUST be pinned: it was previously unpinned, floated to 1.0.0, and
# the old fastapi (<0.110) couldn't accept it — Starlette dropped the on_startup
# kwarg fastapi 0.109 passes to Router, so every APIRouter() raised and the app
# stopped booting. This range is shared by fastapi 0.136, streamlit, and phoenix.
starlette>=1.0.0,<1.1.0
uvicorn[standard]>=0.27.0,<0.28.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
langgraph>=0.0.20,<0.1.0
langchain>=0.1.0,<0.2.0
langchain-core>=0.1.0,<0.2.0
litellm>=1.17.0,<2.0.0
sqlmodel>=0.0.14,<0.1.0
alembic>=1.13.0,<2.0.0
psycopg2-binary>=2.9.9,<3.0.0
asyncpg>=0.29.0,<0.30.0
qdrant-client>=1.7.0,<2.0.0
redis[hiredis]>=5.0.1,<6.0.0
hiredis>=2.3.2,<3.0.0
arize-phoenix>=2.0.0,<3.0.0
opentelemetry-api>=1.22.0,<2.0.0
opentelemetry-sdk>=1.22.0,<2.0.0
python-dotenv>=1.0.0,<2.0.0
python-multipart>=0.0.6,<0.1.0
httpx>=0.26.0,<0.27.0
tenacity>=8.2.3,<9.0.0
structlog>=24.1.0,<25.0.0
python-json-logger>=2.0.7,<3.0.0
pypdf>=6.0.0,<7.0.0
pdfplumber>=0.11.0,<0.12.0
PyMuPDF>=1.24.0,<2.0.0
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
bcrypt>=4.1.2,<5.0.0
packaging>=23.2,<24.0
setuptools<77
pypdf

# ML & Routing (Layer 0 — Benchmark Router)
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
torch>=2.0.0
sentence-transformers>=2.2.0

# Layer 3 — Benchmark-driven kNN router (Batch 2 deps)
duckdb>=1.1.0            # SQL on outcomes.parquet (sub-ms lookups)
datasets>=3.0.0          # HuggingFace datasets loader for corpus build
pyarrow>=15.0.0          # Parquet I/O
optimum[onnxruntime-gpu]>=1.24.0  # encoder ONNX int8 export
onnxruntime-gpu>=1.20.0  # encoder runtime (CUDA + CPU)

# Layer 3 — Structured output + reliability (Batch 4 deps, installed early)
instructor>=1.0.0        # Pydantic-validated LLM output for Stage C arbitrator
pybreaker>=1.0.0         # per-provider circuit breaker
